<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class=" w-full min-h-screen bg-zinc-900 text-white p-5">

        <h5 class="mb-5">Edit your post.</h5>
        <form action="/update/<%= post._id %>" method="POST">
            <textarea placeholder= "What's on your mind ? " class="h-24 p-2 block outline-none w-1/3 resize-none bg-transparent border-zinc-600 border-2 rounded" name="content"><%= post.content %></textarea>
            <div class="flex justify-between items-center w-full">
                <input class="block px-3 w-40 py-2 outline-none bg-yellow-500 text-black rounded mt-3" type="submit" value="Update New Post"></input>
                <a href="/logout" class="mb-5 inline-block px-3 py-2 bg-red-600 rounded-md">Logout</a>
            </div>
        </form>
        

    </div>
</body>

</html>