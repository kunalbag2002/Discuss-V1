<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class=" w-full min-h-screen bg-zinc-900 text-white p-5">
        <h3 class="text-3xl"><span class="font-light">Hey, <%= user.name %></span></h3>
        <h5 class="mb-5">You can create a post.</h5>
        <form action="/post" method="POST">
            <textarea placeholder= "What's on your mind ? "class="h-24 p-2 block outline-none w-1/3 resize-none bg-transparent border-zinc-600 border-2 rounded" name="content"></textarea>
            <div class="flex justify-between items-center w-full">
                <input class="block px-3 w-40 py-2 outline-none bg-blue-500 rounded mt-3" type="submit" value="Create New Post"></input>
                <a href="/logout" class="mb-5 inline-block px-3 py-2 bg-red-600 rounded-md">Logout</a>
            </div>
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post mb-5 p-3 border-1 border-zinc-700 rounded bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.name %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="text-zinc-500"><%= post.likes.length %> likes</small>
                        <div class="btns flex mt-5 gap-4">
                            <a class="text-blue-500" href="/like/<%= post._id%>">
                                <%= post.likes.includes(user._id) ? 'Unlike' : 'Like' %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id%>">Edit</a>
                    </div>
                </div>
                    </div>
                <% }) %>
            </div>

    </div>
</body>

</html>